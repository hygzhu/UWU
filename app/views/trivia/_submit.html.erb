<%= form_for :answer, url: trivia_path do |f| %>
    <%= f.label "Anime name: " %>
    <%= f.text_field :answer_text, :data => {:autocomplete => trivia_autocomplete_song_source_path} %>


    <%= hidden_field_tag :correct_answer, song.source %>

    <%= f.submit "submit"%>
<% end %>